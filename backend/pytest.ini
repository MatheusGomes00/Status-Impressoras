[pytest]
# pythonpath = . faz os pacotes do projeto (readings, collector, ...)
# serem importáveis pelos testes, que rodam com backend/ como raiz.
pythonpath = .
testpaths = tests
asyncio_mode = auto
